if [ -r "~/.path_extra" ] && [ -f "~/.path_extra" ]; then
  echo "tst"
  source "~/.path_extra";
fi
 
# 手动指定 gem 的安装路径
export GEM_HOME="$HOME/.gem"

# 把 gem 的运行程序加进来
export PATH="$GEM_HOME/bin:$PATH"
