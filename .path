if [ -r "$HOME/.path_extra" ] && [ -f "$HOME/.path_extra" ]; then
  source "$HOME/.path_extra";
fi

# 手动指定 gem 的安装路径
export GEM_HOME="$HOME/.gem"

export PIP_BIN="/Users/leosun/Library/Python/3.9/bin"
# 把 gem 的运行程序加进来
export PATH="$GEM_HOME/bin:$PATH"
export PATH="$PIP_BIN:$PATH"
